{% set name = "cuda-nvml-dev" %}
{% set version = "13.2.86" %}
{% set cuda_version = "13.2" %}
{% set platform = "linux-x86_64" %}  # [linux64]
{% set platform = "linux-ppc64le" %}  # [ppc64le]
{% set platform = "linux-sbsa" %}  # [aarch64]
{% set platform = "windows-x86_64" %}  # [win]
{% set target_name = "x86_64-linux" %}  # [linux64]
{% set target_name = "ppc64le-linux" %}  # [ppc64le]
{% set target_name = "sbsa-linux" %}  # [aarch64]
{% set target_name = "x64" %}  # [win]
{% set extension = "tar.xz" %}  # [not win]
{% set extension = "zip" %}  # [win]

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://developer.download.nvidia.com/compute/cuda/redist/cuda_nvml_dev/{{ platform }}/cuda_nvml_dev-{{ platform }}-{{ version }}-archive.{{ extension }}
  sha256: 28ef22331ee9a20b7875c658db21e9364b4a3fecaac15280640154fc9c892cc7  # [linux64]
  sha256: bfc2f61bed9c4da63d0dccc50d150bee861b3b318f7d8b918a241b66b455d541  # [aarch64]
  sha256: 65c9f7eaa5d73993c319b06445b5b9e3bbc8a2b972e21cc231c4656519238cfb  # [win]

build:
  number: 0
  binary_relocation: false
  skip: true  # [osx or ppc64le]

requirements:
  build:
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}
    - {{ stdlib("c") }}
    - arm-variant * {{ arm_variant_type }}  # [aarch64]
    - cf-nvidia-tools 1  # [linux]
  host:
    - cuda-version {{ cuda_version }}
  run:
    - {{ pin_compatible("cuda-version", max_pin="x.x") }}
  run_constrained:
    - arm-variant * {{ arm_variant_type }}  # [aarch64]

test:
  requires:
    - patchelf  # [linux]
  files:
    - test-rpath.sh
  commands:
    - test -f $PREFIX/lib/pkgconfig/nvidia-ml-*.pc                         # [linux]
    - test -f $PREFIX/targets/{{ target_name }}/lib/stubs/libnvidia-ml.so  # [linux]
    - test -f $PREFIX/targets/{{ target_name }}/include/nvml.h             # [linux]
    - bash test-rpath.sh                                                   # [linux]
    - if not exist %LIBRARY_LIB%\{{ target_name }}\nvml.lib exit 1         # [win]
    - if not exist %LIBRARY_INC%\nvml.h exit 1    # [win]

about:
  home: https://developer.nvidia.com/cuda-toolkit
  license_file: LICENSE
  license: LicenseRef-NVIDIA-End-User-License-Agreement
  license_url: https://docs.nvidia.com/cuda/eula/index.html
  summary: NVML native dev links, headers
  description: |
    NVML native dev links, headers
  doc_url: https://docs.nvidia.com/cuda/index.html

extra:
  recipe-maintainers:
    - conda-forge/cuda
